export const metadata = {
  title: "NX CRM - Dashboard",
  description: "Dashboard de leads e funil de vendas do NX CRM.",
};

export default function DashboardLayout({ children }) {
  return children;
}
